import 'package:flutter/material.dart';

class FunitureScr extends StatelessWidget {
  const FunitureScr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
