import 'package:flutter/material.dart';

class FashionScr extends StatelessWidget {
  const FashionScr({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
